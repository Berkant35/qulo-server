node_modules
dist
.env
*.log
flows/

# DB seed/backup dump'ları — credential-türü alanlar (password_hash, token'lar) içerir, repo'ya girmesin
seed_backup_*.json

# TR seed profil fotoğrafları (tools/seed_photos.py çıktısı, ~300 KB × yüzlerce) — Storage'a yüklenir, repo'ya girmesin
seed-photos-cache/

# superpowers SDD calisma alani (ledger, brief, review paketleri) — repoya girmez
.superpowers/
